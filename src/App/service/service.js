const BASIC_URL = 'http://localhost:8080';

export const GET_STUDENTS_URL = `${BASIC_URL}/students`;

export const POST_STUDENT_URL = `${BASIC_URL}/student`;

export const GET_GROUPS_URL = `${BASIC_URL}/groups`;
